{{#each plantCollection.plants as |plant|}}

  <section data-id="{{plant._id}}">
    <h4 data-id="{{plant._id}}" class="plant pointer">
    {{#if plant.nickName}}
      {{plant.nickName}}
    {{else}}
      {{plant.name}}
    {{/if}}
    </h4>
  </section>
{{/each}}
